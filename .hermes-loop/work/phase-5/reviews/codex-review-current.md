# phase: phase-5
# Review: Phase 5 - Context Composer And Accounting
Verdict: FAIL

Findings:
- critical, src/memoryos_lite/public_case_diagnostics.py:179, dropped v3 diagnostics are counted as selected context evidence. `_selected_context_ids()` folds `item_id`, `source_ids`, and `source_refs` from every v3 diagnostic without checking `included` or `dropped`; the current RED test at tests/test_public_benchmarks.py:748 fails with `selected_context_status == "evidence_selected"` for a dropped-only source. This hides context-missing regressions and violates the phase rule that budget-dropped items must not appear selected. Required fix: derive selected ids from included, non-dropped `final_context_trace` rows, or filter diagnostic rows to `included is True` and `dropped is False`, then keep the dropped-diagnostic RED test green.
- high, .hermes-loop/work/phase-5/result.md:139, recorded full-suite verification is stale or no longer true for the current workspace. Fresh review command `uv run pytest tests/test_public_benchmarks.py::test_public_case_diagnostics_does_not_select_dropped_v3_diagnostics -q` exits 1. This invalidates the `387 passed` completion evidence for the current diff. Required fix: fix the regression, rerun the focused failure and full `uv run pytest -q`, and update phase artifacts with current evidence.
- high, .hermes-loop/work/phase-5/result.md:178, .hermes-loop/work/phase-5/result.md:202, milestone reports record `judge_status=not_run` for all LongMemEval and LoCoMo rows. A phase cannot be ACKed as benchmark-usable when no LLM answer/judge milestone was run and the result itself says the slices are diagnostic only. Required fix: keep Phase 5 unACKed as usable, or run explicit LLM answer/judge milestone commands and report judge outcomes case by case.
- high, .hermes-loop/work/phase-5/result.md:177, .hermes-loop/work/phase-5/result.md:201, no same-subset baseline comparison is present; all movement is `new_case_no_baseline`, so fail-to-pass and pass-to-fail are not computable. This violates the active goal of not hiding case-level regressions. Required fix: compare against the relevant pre-change or Phase 4 same-case baseline with stable case ids and report pass-to-fail and fail-to-pass separately.
- medium, .hermes-loop/work/phase-5/result.md:187, the required parallel milestone eval path is not reliable with default run ids; the first LoCoMo run failed from a shared timestamped run-directory race. Required fix: use distinct explicit run ids for parallel milestone commands or make default public-eval run ids process-unique before treating the parallel gate as reliable.

Gate Assessment:
- active goal alignment: FAIL. The implementation improves diagnostic plumbing, but the live selected-context regression and missing movement/judge evidence block a benchmark-usable ACK.
- context bundle usage: PARTIAL PASS. Current `plan_final.md`, `result.md`, and `execute_review.md` cite `.hermes-loop/work/phase-5/context_bundle.md`; `ack.json` and `reflect_phase-5.md` remain known stale and must not be used for ACK.
- anti-demo level: FAIL. The real path is wired, but completion is still diagnostic-only and the recorded milestone evidence explicitly lacks judge and movement data.
- real path wired: PASS with caveat. `MemoryOSService.build_context()` propagates v3 accounting and final-context trace metadata, but public diagnostics currently misread dropped diagnostics as selected context.
- tests and RED evidence: FAIL. RED coverage exists, including a dropped-diagnostic test, but that test fails in the current workspace; recorded full-suite evidence is stale.
- milestone eval adequacy: FAIL. LongMemEval is 17/30, LoCoMo is 0/30, all rows have `judge_status=not_run`, and no fail-to-pass/pass-to-fail comparison exists.
- v1 fallback/v3 default/kernel opt-in: PASS. I found no diff changing `memoryos_memory_arch = "v3"` or `memoryos_agent_kernel = "off"`, and focused tests/artifacts assert explicit v1 exclusion and kernel default-off.
- source grounding and LoCoMo risk: FAIL. The new final-context trace is useful, but `selected_context_status` can still be inflated by dropped source refs. LoCoMo remains 0/30 and temporal/session neighbor diagnostics are not enough to support usable ACK.
