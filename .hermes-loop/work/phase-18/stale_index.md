# phase: phase-18

# Stale Artifact Index

Active goal:
Improve MemoryOS Lite v3 into a benchmark-usable Letta-style agent memory system for LongMemEval and LoCoMo, without demo-only phase completion, without hiding case-level regressions, and without enabling the v3 kernel by default.

Phase-18 directory did not exist at startup. No phase-18 `ack.json`,
`review_verdict.json`, `result.md`, or planning artifacts were present, so
nothing was quarantined.

Runtime artifacts from earlier phases, including untracked phase-17 eval logs
and orchestration state, are not phase-18 ACK evidence unless explicitly cited
and reviewed in current phase artifacts.
