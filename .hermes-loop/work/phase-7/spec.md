# phase: phase-7

# Spec: Benchmark + Evaluation Compatibility

## Goal
Ensure v3 composer/kernel paths can run hard/LME/LoCoMo comparisons.
Add v3 diagnostics to benchmark reports.

## Tasks
1. Extend public_benchmarks.py with v3 diagnostic fields
2. Separate recall hit / archival passage hit / core usage / budget drop in reports
3. Ensure v1/v2 still work
4. Run full eval suite
